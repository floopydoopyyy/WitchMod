/**
 * Client-only (Dist.CLIENT) rendering for WitchMod's custom HUD overlays (Phase D). Classes here reference
 * client-only Minecraft types and must only be class-loaded on the client — they're wired up from
 * {@link com.oliver.witchmod.WitchModClient}, never from common code.
 */
package com.oliver.witchmod.client;
