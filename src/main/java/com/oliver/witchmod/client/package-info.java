/**
 * client-only (Dist.CLIENT) rendering for the mod's custom hud overlays. classes here reference client-only
 * minecraft types and must only be class-loaded on the client — they're wired up from
 * {@link com.oliver.witchmod.WitchModClient}, never from common code.
 */
package com.oliver.witchmod.client;
