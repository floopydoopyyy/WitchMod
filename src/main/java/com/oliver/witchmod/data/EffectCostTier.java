package com.oliver.witchmod.data;

/** minor/moderate/major label — purely descriptive; the real numeric cost lives on the {@link Effect}. */
public enum EffectCostTier {
    MINOR,
    MODERATE,
    MAJOR
}
